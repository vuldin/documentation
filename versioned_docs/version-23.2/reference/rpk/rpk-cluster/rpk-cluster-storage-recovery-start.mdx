---
title: rpk cluster storage recovery start
rpk_version: (rev )
---

Start the topic recovery process.
This command starts the process of restoring topics from the archival bucket.
If the wait flag (--wait/-w) is set, the command will poll the status of the
recovery process until it's finished.

## Usage

```bash
rpk cluster storage recovery start [flags]
```

## Flags

<table>
  <tbody>
    <tr>
      <td>
        <strong>Value</strong>
      </td>
      <td>
        <strong>Type</strong>
      </td>
      <td>
        <strong>Description</strong>
      </td>
    </tr>
    <tr>
      <td>-h, --help</td>
      <td>-</td>
      <td>Help for start.</td>
    </tr>
    <tr>
      <td>--polling-interval</td>
      <td>duration</td>
      <td>
        The status check interval (e.g. '30s', '1.5m'); ignored if --wait is not
        used (default 5s).
      </td>
    </tr>
    <tr>
      <td>--topic-name-pattern</td>
      <td>string</td>
      <td>A regex pattern that restores any matching topics (default ".*").</td>
    </tr>
    <tr>
      <td>-w, --wait</td>
      <td>-</td>
      <td>Wait until auto-restore is complete.</td>
    </tr>
    <tr>
      <td>--config</td>
      <td>string</td>
      <td>
        Redpanda or rpk config file; default search paths are
        ~/.config/rpk/rpk.yaml, $PWD, and /etc/redpanda/`redpanda.yaml`.
      </td>
    </tr>
    <tr>
      <td>-X, --config-opt</td>
      <td>stringArray</td>
      <td>
        Override rpk configuration settings; '-X help' for detail or '-X list'
        for terser detail.
      </td>
    </tr>
    <tr>
      <td>--profile</td>
      <td>string</td>
      <td>rpk profile to use.</td>
    </tr>
    <tr>
      <td>-v, --verbose</td>
      <td>-</td>
      <td>Enable verbose logging.</td>
    </tr>
  </tbody>
</table>
