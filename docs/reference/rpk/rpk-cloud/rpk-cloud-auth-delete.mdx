---
title: rpk cloud auth delete
rpk_version: v23.2.1
---

<head>
  <meta name="title" content="rpk cloud auth delete| Redpanda Docs" />
  <meta name="description" content="rpk cloud auth delete" />
  <link rel="canonical" href="https://docs.redpanda.com/docs/reference/rpk/rpk-cloud/rpk-cloud-auth/rpk-cloud-auth-delete" />
</head>

Delete an rpk cloud authentication (auth).

Deleting a cloud auth removes it from the `rpk.yaml` file. If the deleted
auth was the current auth, rpk will use a default SSO auth the next time
you try to login, and if the login is successful, it will save the auth.

## Usage

```bash
rpk cloud auth delete [NAME] [flags]
```

## Flags

<table>
  <tbody>
    <tr>
      <td>
        <strong>Value</strong>
      </td>
      <td>
        <strong>Type</strong>
      </td>
      <td>
        <strong>Description</strong>
      </td>
    </tr>
    <tr>
      <td>-h, --help</td>
      <td>-</td>
      <td>Help for delete.</td>
    </tr>
    <tr>
      <td>--config</td>
      <td>string</td>
      <td>
        Redpanda or rpk config file; default search paths are
        ~/.config/rpk/rpk.yaml, $PWD, and /etc/redpanda/`redpanda.yaml`.
      </td>
    </tr>
    <tr>
      <td>-X, --config-opt</td>
      <td>stringArray</td>
      <td>
        Override rpk configuration settings; '-X help' for detail or '-X list'
        for terser detail.
      </td>
    </tr>
    <tr>
      <td>--profile</td>
      <td>string</td>
      <td>rpk profile to use.</td>
    </tr>
    <tr>
      <td>-v, --verbose</td>
      <td>-</td>
      <td>Enable verbose logging.</td>
    </tr>
  </tbody>
</table>
