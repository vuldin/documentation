---
title: rpk cluster license set
rpk_version: v23.2.1
---

<head>
  <meta name="title" content="rpk cluster license set | Redpanda Docs" />
  <meta name="description" content="rpk cluster license set" />
  <link rel="canonical" href="https://docs.redpanda.com/docs/reference/rpk/rpk-cluster/rpk-cluster-license-set/" />
</head>

Upload license to the cluster.

You can either provide a path to a file containing the license:

```bash
rpk cluster license set --path /home/organization/redpanda.license
```

Or inline the license string:

```bash    
rpk cluster license set <license string>
```

If neither are present, `rpk` will look for the license in the
default location `/etc/redpanda/redpanda.license`.

## Usage

```bash
rpk cluster license set [flags]
```

## Flags

<table>
  <tbody>
    <tr>
      <td>
        <strong>Value</strong>
      </td>
      <td>
        <strong>Type</strong>
      </td>
      <td>
        <strong>Description</strong>
      </td>
    </tr>
    <tr>
      <td>-h, --help</td>
      <td>-</td>
      <td>Help for set.</td>
    </tr>
    <tr>
      <td>--path</td>
      <td>string</td>
      <td>Path to the license file.</td>
    </tr>
    <tr>
      <td>--config</td>
      <td>string</td>
      <td>
        Redpanda or rpk config file; default search paths are
        ~/.config/rpk/rpk.yaml, $PWD, and /etc/redpanda/`redpanda.yaml`.
      </td>
    </tr>
    <tr>
      <td>-X, --config-opt</td>
      <td>stringArray</td>
      <td>
        Override rpk configuration settings; '-X help' for detail or '-X list'
        for terser detail.
      </td>
    </tr>
    <tr>
      <td>--profile</td>
      <td>string</td>
      <td>rpk profile to use.</td>
    </tr>
    <tr>
      <td>-v, --verbose</td>
      <td>-</td>
      <td>Enable verbose logging.</td>
    </tr>
  </tbody>
</table>
