---
title: rpk cluster config import
rpk_version: v23.2.1
---

<head>
  <meta name="title" content="rpk cluster config import | Redpanda Docs" />
  <meta name="description" content="rpk cluster config import" />
  <link rel="canonical" href="https://docs.redpanda.com/docs/reference/rpk/rpk-cluster/rpk-cluster-config-import/" />
</head>

:::caution
Redpanda does not support importing cluster-specific identification (such as `cluster_id`) with this command.
:::

Import cluster configuration from a file.

Import configuration from a YAML file, usually generated with
corresponding `export` command. This downloads the current cluster
configuration, calculates the difference with the YAML file, and
updates any properties that were changed. If a property is removed
from the YAML file, it is reset to its default value.

## Usage

```bash
rpk cluster config import [flags]
```

## Flags

<table>
  <tbody>
    <tr>
      <td>
        <strong>Value</strong>
      </td>
      <td>
        <strong>Type</strong>
      </td>
      <td>
        <strong>Description</strong>
      </td>
    </tr>
    <tr>
      <td>-f, --filename</td>
      <td>string</td>
      <td>full path to file to import, e.g. '/tmp/config.yml'.</td>
    </tr>
    <tr>
      <td>-h, --help</td>
      <td>-</td>
      <td>Help for import.</td>
    </tr>
    <tr>
      <td>--all</td>
      <td>-</td>
      <td>Include all properties, including tunables.</td>
    </tr>
    <tr>
      <td>--config</td>
      <td>string</td>
      <td>
        Redpanda or rpk config file; default search paths are
        ~/.config/rpk/rpk.yaml, $PWD, and /etc/redpanda/`redpanda.yaml`.
      </td>
    </tr>
    <tr>
      <td>-X, --config-opt</td>
      <td>stringArray</td>
      <td>
        Override rpk configuration settings; '-X help' for detail or '-X list'
        for terser detail.
      </td>
    </tr>
    <tr>
      <td>--profile</td>
      <td>string</td>
      <td>rpk profile to use.</td>
    </tr>
    <tr>
      <td>-v, --verbose</td>
      <td>-</td>
      <td>Enable verbose logging.</td>
    </tr>
  </tbody>
</table>
